
!/memory/agents/README.md
!/memory/sessions/README.md
!bin/claude-flow
!v3/@claude-flow/
# Binaries
# Build outputs

# Claude-Flow specific
# Dependencies
# Development
# Dot folders to ignore (except .claude, .claude-plugin, .git, .github, .githooks)
# Environment
# Exclude @claude-flow module directories from ignore patterns
# IDE
# Logs
# MCP/Swarm
# OS
# Python
# Removed Windows wrapper files
# SPARC/Roo files (not used)
# SQLite Database Files
# Temp files
# Temporary files
# Test artifacts
# Test outputs
# Testing
# memory/
# sessions/
*$py.class
*.db
*.db-journal
*.db-wal
*.egg
*.egg-info/
*.exe
*.lcov
*.log
*.pid
*.pid.lock
*.py[cod]
*.seed
*.so
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.swo
*.swp
*.temp
*.tmp
*.tsbuildinfo
*~
./claude/checkpoints/
.DS_Store
.Python
.agentic-flow/
.cache/
.claude-flow/
.claude/settings.local.json
.deno/
.env
.env.*.local
.env.local
.hive-mind/
.hive-mind/hive.db*
.idea/
.mcp.json
# Plugin-shipped .mcp.json templates are part of the plugin contract (smoke
# step "registers a 'ruflo' MCP server" asserts they're committed). Exempt
# them from the repo-level .mcp.json ignore. User-specific .mcp.json at
# the repo root or other paths stays ignored.
!plugins/*/.mcp.json
.nyc_output/
.pnpm-debug.log*
.pnpm-store/
.research/
.roo/
.roomodes
.ruv-swarm/
.swarm/
.swarm/memory.db*
.turbo/
.vscode/
/coordination/
/memory/
/memory/agents/*
/memory/claude-flow-data.json
/memory/sessions/*
Thumbs.db
__pycache__/
analyzing
api-project/
bin/claude-flow-*
build/
claude-flow
claude-flow-mcp-wrapper
claude-flow.config.json
claude-flow.db
coordination/memory_bank/*
coordination/orchestration/*
coordination/subtasks/*
coverage/
dist-cjs/
dist/
hive-mind-prompt-*.txt
init-output.log
lerna-debug.log*
logs/
mcp-server.log
memory/backups/
node_modules/
npm-debug.log*
out/
output/
package
pnpm-debug.log*
swarm-debug.log
swarm-runs
test-full-init
test-init
test-output/
test-reports/
tests/logs/
tests/results/
the
tmp/
ui_*.log
yarn-debug.log*
yarn-error.log*

# Claude Flow generated files
/coordination/memory_bank/*
/coordination/subtasks/*
/coordination/orchestration/*
# Removed Windows wrapper files per user request

# Codex local configuration
.codex/

# Claude Flow runtime data
.claude-flow/data/
.claude-flow/logs/

# Environment variables — block ALL .env variants except .example templates
.env.*
!.env.example
!.env.*.example

# ============================================================================
# SECRETS — keys, tokens, credentials, env. Mọi dữ liệu nhạy cảm phải ignore;
# bản mẫu *.example luôn được phép commit (negation cuối section)
# ============================================================================

# Private keys & keystores
*.pem
*.key
*.p8
*.pkcs8
*.pkcs12
*.p12
*.pfx
*.ppk
*.jks
*.keystore
*.jceks
*.kdb
*.kdbx
*.keychain
*.keychain-db
*.age
*.sec
*.gpgkey
id_rsa
id_ed25519
id_ecdsa
id_dsa
*.id_rsa
*.id_ed25519

# Credentials & token files
.netrc
.git-credentials
.htpasswd
*.htpasswd
.pypirc
.npmrc
.yarnrc
.yarnrc.yml
.pnpmrc
.envrc
.pgpass
.my.cnf
.gitconfig
*.token
*.tokens
*.secret
secrets.*
*credentials.json
*service_account*.json
*service-account*.json
client_secret*.json

# Cloud & infra state (có thể chứa secret)
.aws/
.gcp/
.azure/
.kube/
kubeconfig
*.kubeconfig
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json

# Vault & misc
.vault-token
vault_token
*.saml
*.oidc

# Shell histories — có thể chứa token gõ tay
.bash_history
.zsh_history
.zsh_sessions/
.python_history
.psql_history
.mysql_history

# Bản mẫu .example LUÔN được phép commit
!*.example

# Publish-artifact mirror — regenerated by prepublishOnly (package.json:90); never edit or commit
v3/@claude-flow/cli/plugins/

# Runtime artifacts of the version-stamped helper auto-refresh (ADR-174) —
# regenerated on demand, not source. The signed manifest source of truth lives
# in v3/@claude-flow/cli/.claude/helpers/helpers.manifest.json.
.claude/helpers/.helpers-version
.claude/helpers/helpers.manifest.json

# Runtime proven-config adoption state (per-install, not source)
.claude/proven-config.json
.claude/.proven-config-version
**/.claude/proven-config.json
**/.claude/.proven-config-version
HLK/config/secrets.*
!HLK/config/secrets.env.example
HLK/config/*.local.json
HLK/logs/
*.rvf
*.rvf.lock
agentdb.rvf
agentdb.rvf.lock
example.env
HLK/dist/*.tgz
HLK/dist/

# Node portable — runtime tooling auto-refreshed (ADR-174); toàn bộ .tools là
# runtime artifact, không bao giờ commit (không có file source nào tracked)
.tools/

# Reference repos (research clones — never commit, move to temp after use)
*-ref/

# Removed upstream ruflo source (Option B surgical cleanup)
v3/
plugins/
ruflo/
.claude-plugin/

# Allow source-audit-fix plan documents only, ignore other docs/ content
!docs/plans/
!docs/plans/source-audit-fix/
!docs/plans/source-audit-fix/**
docs/plans/*/
docs/*
!docs/reports/
!docs/reports/**

verification/
services/
tests/
crates/
scripts/

# aide-memory
.aide/memories/preferences/personal/
.aide/cache/
.aide/recall-log.jsonl
.aide/pending-memories.jsonl

# Backup directories
.devin.backup_*/

# Runtime state directories
# Lưu ý: .devin/plan_state là symlink (harness boot) — pattern KHÔNG có
# trailing slash để match cả symlink lẫn thư mục thật
.devin/plan_state
.devin/session_state/
.devin/context_flags/
.devin/loop_state_archive/
.devin/blackboard/
.devin/event_bus/
.devin/state/
.devin/telemetry/
.devin/artifacts/
.devin/agents_state

# Runtime orchestrator state — symlink tới state/ (harness boot tạo, git
# không follow symlink nên pattern thường không match; anchor root cho chắc)
/agents
/plan

# Allow current project tests and scripts
!tests/
!tests/**
!.devin/scripts/
!.devin/scripts/**
# Keep pycache ignored even inside un-ignored script dirs
.devin/scripts/**/__pycache__/
.devin/scripts/plan_fsm/__pycache__/

# Local permission config (per-machine, do not commit)
.devin/config.local.json

# Worktrees are runtime artifacts
.worktrees/

# Hook integrity generated timestamp (hash baseline stays tracked)
.devin/hook_hashes_generated.json

tests/__pycache__/

# Remaining ruflo artifacts
.agents/
.harness/
bin/
data/
plugin/
!.opencode/plugin/
!.opencode/plugin/*.ts


# Editor configs recreated by aide-memory hooks (Devin-only workspace)
.claude/
.cursor/

.cursor/rules/aide-memory.mdc

# Backups (U07: disaster recovery)
backups/

# Runtime/test artifacts generated during AHD pytest runs
.coverage
.pytest_cache/
.serena/
state/
.devin/artifact_registry/
.devin/checkpoints/
.devin/idempotency/
.devin/hook_order.json

