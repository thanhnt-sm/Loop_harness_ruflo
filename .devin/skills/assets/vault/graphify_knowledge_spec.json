{
  "$schema": "https://agent-harness-deploy.dev/vault/graphify_knowledge_spec.v1.json",
  "vault_version": "1.0.0",
  "vault_purpose": "Anti-link-rot local cache of structural code-to-knowledge graph parsing definitions. Hardcoded from safishamsi/graphify code-to-knowledge-graph patterns. No runtime fetch — this file IS the schema.",
  "source_references": [
    "https://github.com/safishamsi/graphify"
  ],
  "core_concept": "Transform a codebase into a queryable knowledge graph where nodes are code entities (files, functions, classes, variables) and edges are relationships (calls, imports, defines, depends-on). The graph enables the harness to answer structural questions without reading every file.",
  "graph_schema": {
    "node_types": [
      {
        "type": "file",
        "properties": ["path", "language", "line_count", "last_modified", "git_sha"],
        "description": "A source file in the repository."
      },
      {
        "type": "function",
        "properties": ["name", "file_path", "start_line", "end_line", "signature", "return_type", "visibility"],
        "description": "A function or method definition."
      },
      {
        "type": "class",
        "properties": ["name", "file_path", "start_line", "end_line", "superclass", "interfaces", "visibility"],
        "description": "A class or struct definition."
      },
      {
        "type": "variable",
        "properties": ["name", "file_path", "line", "type", "scope", "mutability"],
        "description": "A variable, constant, or field declaration."
      },
      {
        "type": "module",
        "properties": ["name", "path", "exports", "imports"],
        "description": "A module or package boundary."
      },
      {
        "type": "config_entry",
        "properties": ["key", "file_path", "value_type", "value"],
        "description": "A configuration entry (JSON/YAML/TOML/ENV)."
      },
      {
        "type": "doc",
        "properties": ["path", "title", "section_count", "word_count", "last_modified"],
        "description": "A documentation file."
      }
    ],
    "edge_types": [
      {
        "type": "calls",
        "from": "function",
        "to": "function",
        "properties": ["call_line", "call_type (direct/callback/dynamic)"],
        "description": "Function A calls function B."
      },
      {
        "type": "imports",
        "from": "file|module",
        "to": "file|module",
        "properties": ["import_line", "import_type (named/default/namespace/side-effect)"],
        "description": "File A imports from file B."
      },
      {
        "type": "defines",
        "from": "file",
        "to": "function|class|variable",
        "properties": [],
        "description": "File A defines entity B."
      },
      {
        "type": "depends_on",
        "from": "file|module",
        "to": "file|module",
        "properties": ["dependency_type (build/runtime/test/dev)"],
        "description": "File or module A depends on B (transitive of imports + package deps)."
      },
      {
        "type": "implements",
        "from": "class",
        "to": "class|interface",
        "properties": [],
        "description": "Class A implements interface B or extends class B."
      },
      {
        "type": "references",
        "from": "function|class|variable",
        "to": "variable|function|class",
        "properties": ["reference_line", "reference_type (read/write/declaration)"],
        "description": "Entity A references entity B (variable usage, type annotation, etc.)."
      },
      {
        "type": "documents",
        "from": "doc",
        "to": "file|function|class|module",
        "properties": ["section", "link_type (inline/external)"],
        "description": "Documentation A describes entity B."
      }
    ]
  },
  "parsing_rules": {
    "javascript_typescript": {
      "parser": "tree-sitter (typescript grammar) or ts-morph",
      "extract": ["import declarations", "function/method definitions", "class definitions", "variable declarations (const/let/var)", "export statements", "call expressions", "type annotations"],
      "edge_generation": "imports → imports edge, call expressions → calls edge, class heritage → implements edge, variable usage → references edge"
    },
    "python": {
      "parser": "tree-sitter (python grammar) or ast module",
      "extract": ["import statements", "def definitions", "class definitions", "assignment statements", "decorator usage", "call expressions"],
      "edge_generation": "imports → imports edge, calls → calls edge, class bases → implements edge, name usage → references edge"
    },
    "json_yaml_toml": {
      "parser": "native JSON parser / PyYAML / tomllib",
      "extract": ["top-level keys as config_entry nodes", "nested keys as config_entry nodes with dotted path", "cross-references to file paths as references edges"],
      "edge_generation": "key values containing file paths → references edge to file node"
    },
    "markdown": {
      "parser": "markdown-it or remark",
      "extract": ["headers as section nodes", "code blocks with file paths as references", "inline links to files as documents edges"],
      "edge_generation": "links to source files → documents edge, code block language tags → language property on file nodes"
    }
  },
  "query_patterns": [
    {
      "name": "impact_analysis",
      "question": "If I change file X, what else breaks?",
      "graph_query": "Find all nodes with an imports or depends_on edge TO file X. Recursively expand for transitive dependencies.",
      "use_case": "Before editing a shared module, understand the blast radius."
    },
    {
      "name": "dead_code_detection",
      "question": "Which functions are never called?",
      "graph_query": "Find all function nodes with zero incoming calls edges. Filter out exported functions (they may be called externally).",
      "use_case": "Cleanup, refactoring scope."
    },
    {
      "name": "circular_dependency",
      "question": "Are there circular import chains?",
      "graph_query": "Detect cycles in the imports edge graph (DFS with visited set).",
      "use_case": "Architecture health check."
    },
    {
      "name": "orphan_documentation",
      "question": "Which docs reference files that no longer exist?",
      "graph_query": "Find documents edges where the target file node doesn't exist in the graph.",
      "use_case": "Doc maintenance, link-rot detection."
    },
    {
      "name": "entry_point_tracing",
      "question": "What's the call chain from entry point to function X?",
      "graph_query": "BFS/DFS from entry point node following calls edges until target function is reached.",
      "use_case": "Debugging, understanding control flow."
    },
    {
      "name": "config_dependency",
      "question": "Which code reads config key X?",
      "graph_query": "Find config_entry node for key X. Find all references edges from function/class nodes to this config_entry.",
      "use_case": "Config change impact, dead config detection."
    }
  ],
  "harness_integration": {
    "scout_worker": "Scout can query the knowledge graph instead of reading files directly. 'Find all callers of function X' → graph query, not 10 file reads.",
    "gap_scan": "The gap-scan skill can use the graph to identify untested code paths (functions with no test references).",
    "auditor": "The Auditor can check for circular dependencies, dead code, and orphan docs via graph queries.",
    "memory": "The knowledge graph can be persisted as a cold-layer artifact, enabling cross-session structural queries without re-parsing."
  },
  "build_and_refresh": {
    "trigger": "On first deploy, after significant code changes (>5 files modified), or on explicit request.",
    "method": "Parse all source files per language rules → generate nodes and edges → serialize to JSON graph file at .devin/knowledge_graph.json",
    "incremental": "On file change, re-parse only the changed file and update its nodes/edges. Full rebuild only on explicit request or schema change.",
    "storage": ".devin/knowledge_graph.json (cold layer, grep/query only, not read in full at BOOT)"
  },
  "honest_clause": "If the codebase uses a language not in parsing_rules, label the graph as 'partial — language X not parsed'. Do not fabricate edges for unparsed files. Report coverage percentage.",
  "integration_points": {
    "canon": ".devin/canon/ (graph is a cold-layer tool, not a canon rule)",
    "docs": "Docs/01-Architecture.md (mentioned as optional structural analysis tool)",
    "skill": ".devin/skills/ (future graphify skill can wrap this schema)",
    "entry_injection": "Not injected into entry files — the graph is an optional analysis layer, not a mandatory rule"
  }
}
