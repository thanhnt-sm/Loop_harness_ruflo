# User Taste Profile
- Communicates in Vietnamese (Tiếng Việt); expects responses in Vietnamese for content but keeps code, paths, errors, and technical identifiers in English/verbatim. Confidence: 0.95
- Prefers Caveman-style communication: cut filler, keep code/paths/errors verbatim, no sugarcoating. Confidence: 0.85
- Wants parallel/song-song execution by default when work is decomposable. Confidence: 0.9
- Wants M+ tier tasks (anything non-trivial: multi-file, destructive risk, architectural) to ALWAYS go through `/full-power` 3-phase flow: Plan → Approve → Execute. Confidence: 0.95
- Wants S-tier tasks (single file, <5 lines, no destructive side effects) to be fixed directly, skipping to report. Confidence: 0.9
- Wants a deterministic gate (governance check + hook integrity + relevant tests) run BEFORE any "done" claim. Confidence: 0.95
- Wants adversarial/red-team review with Tier 1 (Critical) findings fixed in-session, not deferred to backlog. Confidence: 0.9
- Wants the full 18-step chain (BOOT → INVENTORY → COMMANDER_MODE → TIER → PREFLIGHT → BRAINSTORM → 8 SCOUTs → ARCHITECT → adversarial → SDD approval → DECOMPOSE → GAP_SCAN → QC → PLAN_ENHANCE → plan approval → EXECUTE → MEMORY+NUWA+REPORT → APPLY) — no steps skipped, no shortcuts. Confidence: 0.9
- Wants MAX-POWER / FULL-COVERAGE: load all relevant mod files (canon, skills, agents) and dispatch all relevant agents (harness + personas + workers), even when task is small. Confidence: 0.85
- Never edit `HLK/`, `.env`, `HLK/config/**`, `HLK/secrets/**`, or any security policies — unless the task is explicitly an HLK upgrade. Confidence: 0.98
- Never run destructive commands: `rm -rf /`, `rm -rf ~`, force-push (`git push -f/--force`), `git reset --hard`, `git clean -fd`, `git branch -D`, `git checkout -- .`, `curl|sh`, `wget|sh`, `mkfs`, `dd if=`, `shred`, `chmod -R 777`, `chown`, `shutdown`, `reboot`, `halt`, `poweroff`, `swapoff`, `cryptsetup luksFormat/erase`. Confidence: 0.98
- Always asks confirmation before destructive operations (force-push, reset, etc.) via a permission gate, not just deny-by-default. Confidence: 0.85
- Prefers smallest coherent diff — no scope creep, no drive-by refactors outside the task. Confidence: 0.9
- Reports go to canonical location: `docs/reports/<SUBJECT>_<YYYY-MM-DD>.md` for ad-hoc reports, or `docs/plans/<slug>/EXECUTION_REPORT.md` for plan-driven tasks. Confidence: 0.9
- Prefers mirror/bridge pattern over rewrite: when adding a new provider/CLI layer (e.g., cmdc wrapping .devin + .opencode), mirror schemas and contracts, do not duplicate content as source of truth. Confidence: 0.9
- Prefers thin index/wrapper files (1-line forwarders to source of truth) over deep copies of large content. Confidence: 0.85
- Prefers fail-open bridge contracts (hooks forward events but never block the caller on latency or error). Confidence: 0.85
- Wants workspace to be cross-provider compatible: skills, MCP servers, hooks, agents, commands registered in each provider's native discovery path (cmdc, opencode, devin). Confidence: 0.9
- Prefers POSIX sh (`#!/usr/bin/env bash`) hook shims for portability, with Windows + Unix python resolution (`.venv/Scripts/python.exe`, `.venv/bin/python`, `python`, `python3`). Confidence: 0.85
- Prefers JSON-stdin/JSON-stdout hook contract with `hookSpecificOutput.permissionDecision` for PreToolUse. Confidence: 0.9
- Prefers explicit schema headers in JSON config files (e.g., `"$comment": "..."` with reference URL). Confidence: 0.8
- Prefers clear hard invariants at top of skill/command docs (numbered, non-negotiable list). Confidence: 0.85
- Prefers a tier-classification gate (S/M/L/XL) chosen by size, file count, destructiveness — not by user judgment. Confidence: 0.9
- Prefers to ask user (via `ask_user_question`) when apply mode is ambiguous (direct apply vs. /full-power vs. dry-run), rather than guessing. Confidence: 0.85
- Prefers to confirm scope (minimum / full / full+personas) before applying multi-file changes. Confidence: 0.85
- Prefers loading mods/canon files lazy/on-demand per phase, not all-at-once (avoid context rot). Confidence: 0.8
- Wants agents dispatched in parallel via `run_in_background=true` (or equivalent) for review-type personas (architect, saboteur, code-reviewer, security-auditor). Confidence: 0.9
- Wants ≥2 personas running in parallel during adversarial review step, then aggregate findings by severity. Confidence: 0.85
- Prefers separating read-only verifier (fable-judge style done-gate) from executors. Confidence: 0.85
- Prefers persona agents to be READ-ONLY (no edit/write), explicit in frontmatter. Confidence: 0.8
- Wants adversarial findings categorized as Critical/High/Medium/Low with structural-hypothesis column (not just severity). Confidence: 0.85
- Wants verification report to include: verdict, evidence (with `file:line`), next-step suggestion. Confidence: 0.9
- Wants governance check (`tools/check_governance.py`) to be the first verification step; treats errors=0 as baseline. Confidence: 0.9
- Prefers 5-Whys root-cause analysis for any FAIL finding rather than just patching symptoms. Confidence: 0.8
- Prefers end-of-task reports with: baseline state, after state (with file tree + sizes), verification verdict, next candidates (ranked), restart guide if any config requires session reload. Confidence: 0.9
- Prefers reports written in markdown with tables for scannable comparison (e.g., before/after, file list with byte sizes). Confidence: 0.85
- Prefers `date -u +%FT%TZ` (ISO 8601 UTC) timestamps in audit logs. Confidence: 0.8
- After applying config changes (settings.json, MCP, hooks), wants user reminded to restart the session for changes to take effect. Confidence: 0.85
- Treats the workspace as a **three-layer harness** (`.devin/` = AHD canon, `.opencode/` = provider wrappers, `HLK/` = security & knowledge protection). Each layer is a first-class concern with its own agent/hook/command wiring — not just an afterthought. Confidence: 0.9
- Wants the HLK security layer wired at **MAX POWER / MAX PERFORMANCE** by default: HLK sanitizer hook in PreToolUse + PostToolUse shell (positioned as line 2 in hook chain, after deny-dangerous and before devin bridge), HLK git-tools pre-flight before commit/push, HLK integrity check before any "done" claim, HLK self-test as part of every full-power preflight. Confidence: 0.9
- Prefers a **layered hook chain** in PreToolUse shell with explicit ordering: (1) deny-dangerous → (2) HLK sanitizer + telemetry blocker → (3) bridge forwarder. Each layer has a single responsibility and fail-open contract. Confidence: 0.85
- Wants each security/protection layer to expose its own **dedicated operator agent** (e.g., `hlk-engineer`) that wraps the layer's scripts/wrappers/prompts/skills into a single dispatchable role with read-only default. Confidence: 0.85
- Wants HLK as a hard preflight dependency: `hlk-status --self-test` + `hlk-verify-integrity.js` must pass before any plan/execute phase, not just on user request. Confidence: 0.85
- Prefers per-layer slash command namespaces (e.g., `/hlk-*` for HLK surface) so commands are scannable and route by domain. Confidence: 0.8
- Wants analyst/prompt libraries (e.g., HLK's 6 `*.prompt.md` files for codebase_analysis, redteam_security, solution_architect, data_leak_hardening, harness_deepdive, hardening_implementation) wrapped behind a single command (`/hlk-prompts <num|keyword>`) for dispatch. Confidence: 0.8
- Writes informally with typos (e.g., "kiểm soát chất lượn", "chaast lượng", "max performance") — does not expect correction; expects the assistant to interpret intent and proceed. Confidence: 0.85
- Prefers the assistant to **catch gaps proactively and extend scope** (e.g., when user says "sử dụng HLK", expand to MAX POWER with all HLK components wired, not just a minimal addition). Confidence: 0.85
- Wants cross-layer feature inventory tables in reports (feature | status | access path) to make wiring completeness auditable at a glance. Confidence: 0.85
- Wants comprehensive reference documentation built and saved to canonical paths (e.g., `docs/reports/<TOPIC>_FULL_GUIDE.md` + `docs/<TOPIC>_QUICKREF.md`) for ongoing use — not just ad-hoc answers in chat. Treats documentation as a durable artifact, not a conversation artifact. Confidence: 0.9
- Prefers documentation as a **two-file pair**: one-page quick reference (printable, table-heavy) for fast lookup + full guide (multi-section, deep-dive) for comprehensive understanding. Keeps the quick ref scannable in <1 minute. Confidence: 0.9
- Wants a **chain decision table** in documentation: maps task characteristics (size, file count, complexity, security) → recommended chain/command. Eliminates guesswork about which command to invoke. Confidence: 0.85
- Wants full **end-to-end loop procedure** documented as a numbered sequence: baseline check → run chain → approve gates → execute → loop iterate → git doctor → commit → push. Makes the maximum-flow procedure reproducible by anyone. Confidence: 0.85
- Wants proactive **security hardening to maximum** when given a security/audit request: don't just review — research best practices, enumerate gaps in a table with severity, fix them, verify, and write a structured audit report. Treats "audit + research best practices" as a single composite task. Confidence: 0.9
- Wants `.commandcode/settings.json` permission rules treated as a first-class security surface: explicit `disableBypass: "disable"` (block `--yolo`/`--dangerously-skip-permissions`), explicit `disableSkillShellExecution: true` (block inline `!cmd` in SKILL.md), explicit `additionalDirectories` list for audit clarity. Confidence: 0.9
- Wants comprehensive **sensitive-path deny coverage**: secret material (`.env`, `~/.ssh/**`, `~/.aws/**`, `~/.gnupg/**`, `~/.kube/**`, `secrets/**`), persistence vectors (`.bashrc`, `.zshrc`, `.profile`, `.gitconfig`, `.gitmodules`, `.mcp.json`, `.commandcode/settings*.json`), control surfaces (`.git/**`, HLK `config/wrappers/bin/security/custom-hooks`), filesystem root + home (`rm -rf /`, `rm -rf ~`, `rm -rf $HOME`). Confidence: 0.95
- Wants MCP tool permissions to be **allowlisted by specific tool name** (e.g., `mcp__aide-memory__aide_recall`), not wildcard `mcp__*` — narrow as tools become known, defer with reason when unknown. Confidence: 0.9
- Wants a `.commandcode/settings.local.json` (gitignored) template for personal overrides that cannot be committed — follows the same pattern as `.claude/settings.local.json`. Confidence: 0.85
- Wants **defense-in-depth hook chain**: deny-dangerous → schema/path-validate → HLK sanitizer → bridge-forwarder. Each layer redundant (same rule enforced at multiple layers), fail-open on conflict, deny rule at cmdc engine level always wins. Confidence: 0.9
- Wants network egress (WebFetch, WebSearch) and arbitrary code execution (`node *`, `npx *`, `python -c *`, `npm install`, `pip install`) in the `ask` list by default, not `allow`. Defaults to prompting over silent grant. Confidence: 0.85
- Wants security audit reports to enumerate gaps as a numbered table with: gap description, severity (CRITICAL/HIGH/MEDIUM/LOW), and status (fixed/deferred with reason). Produces an actionable, scannable artifact rather than prose. Confidence: 0.9
- Wants the assistant to **self-apply known best-practice hardening** in S/M-tier scope (1-file config rewrites like `settings.json`) without escalating to `/full-power` — security hardening is treated as deterministic, not architectural. Confidence: 0.85
- Wants `.gitignore` updated to include local-only config files (`.commandcode/settings.local.json`) when those files are introduced — follows the existing pattern of `.claude/settings.local.json`. Confidence: 0.85
- Wants custom agents to have **fully pinned frontmatter** (model ID per tier, maxTurns cap, permissionMode, background, showOutput, disallowedTools) — not just `model: inherit`. Personas = `permissionMode: plan` + `disallowedTools: shell_command, write_file, edit_file` to enforce true read-only. Confidence: 0.9
- Wants agents **tiered by model** to optimize cost: commander = opus (deep reasoning), persona = sonnet (review lens), executor = free tier (glm/kimi) or paid (sonnet), verifier/scheduler = haiku (cheap, read-only). Prompt cache per agent avoids thrash between slow planner and fast implementer. Confidence: 0.85
- Wants Mods (ModApi, TypeScript plugin) used as a **first-class extension surface** for cmdc — slash commands + inline tools + lifecycle observers in one file, loaded via `cmd --mod <path>` or `.commandcode/mods/`. Prefers writing a TS mod over duplicating hook/skill logic in multiple places. Confidence: 0.85
- Wants project memory at **tier 2 (`.commandcode/AGENTS.md`)** in addition to root `AGENTS.md` — captures cmdc-specific surface (skills, MCP, hooks, agents, commands, mods) and hard guards that bind for the project. Uses `@path` imports to cross-reference canonical files. Confidence: 0.85
- Wants **explicit skill frontmatter flags** (`disable-model-invocation`, `user-invocable`, `argument-hint`, `allowed-tools`) set deliberately per skill — never left implicit. Read-only skills (like index/bridge) get `allowed-tools: Read` to scope execution. Confidence: 0.8
- Wants `onDemandToolDescriptions: true` + `disableScratchpad: false` + `autoCompact: true` set explicitly in `settings.json` (not just left at defaults) so the values are auditable in the file. Confidence: 0.8
- Wants research reports to include a **competitive comparison table** (cmdc vs opencode vs claude-code vs AHD) that maps which provider has which feature — surfaces unique-to-cmdc capabilities (session tree, scratchpad, compaction append-only, ModApi) at a glance. Confidence: 0.85
- Wants research output to identify **unique/distinctive features** of the tool (not just features used) and document them — treats the report as a capability inventory, not just a usage log. Confidence: 0.85
- Wants research reports to end with a **"next candidates" section** (ranked, with score) listing unimplemented improvements — turns research into a backlog, not a one-shot artifact. Confidence: 0.85
- When the assistant offers scope options (e.g., "Tất cả / Lõi thôi / Skip"), prefers the user to be able to pick **full MAX POWER scope** — when the user has expressed MAX PERFORMANCE intent, treats it as binding across all subsequent phases. Confidence: 0.9
- Wants a verification script that loops over all custom agents and asserts that each has the MAX POWER frontmatter fields (model, maxTurns, background, permissionMode) — treats "all agents fully configured" as a checked invariant, not an assumption. Confidence: 0.85
- Holds the philosophy that **the biggest problem in AI coding is verifying code, not generating it** — if an agent can't run the product itself, click the UI, open the simulator, or reproduce a bug from a screenshot/description, the human remains the bottleneck. Wants harness/agents designed around self-verification capability, not just generation speed. Confidence: 0.95
- Wants **environment + process + acceptance criteria built FIRST**, then let the swarm of agents work — opposite of "let agent try and fix later". Prefers up-front investment in testable harness over reactive patching. Confidence: 0.95
- Wants every agent failure mode (guess, misread, wrong direction) captured as a **new skill** — then tested like code: parallel sub-agents, rubric-based scoring, cross-model grading. Treats the skill library as a living artifact that grows from failure. Confidence: 0.9
- Holds the **"20 PRs in main by morning"** vision: agents authorized to merge PRs after passing CI/auto-check, so the human reviews results, not process. Wants CI gate + auto-merge as default end-state of the harness. Confidence: 0.9
- For permission bypass: prefers **session-only bypass** (CLI flag at launch, no config mutation, no persistence) over config-file changes — avoids polluting `opencode.json`/`.commandcode/settings.json` with permanent allow rules. Confidence: 0.9
- Wants YAML frontmatter of all agents/commands validated programmatically (loop over files, `yaml.safe_load`, report OK/FAIL with file path) as part of harness hygiene — treats parser-validity as a checked invariant, not a "should work" assumption. Especially for files containing colons inside string descriptions, which silently break YAML. Confidence: 0.9
- When a config file (e.g., `opencode.json`) controls permission surface and bypass flag cannot be set mid-session, prefers **inline option + explicit restart reminder** over mutating the persistent config — keeps the security surface stable across sessions. Confidence: 0.85
- For auto-generated artifacts (skills, PRs, agents), wants **multi-source consensus before promotion** — minimum 2 different executors/models must observe the same pattern, plus a minimum occurrence count + fail-rate threshold. Anti-abuse: a single model's pattern is not enough evidence. Confidence: 0.9
- For auto-merge flows, wants a **blast-radius limit** expressed as max N PRs/day/branch-prefix + an explicit `allowed_title_prefixes` allowlist + a **file-based kill switch** (e.g., touch `.devin/state/auto_pr_disabled` to disable, rm to re-enable). Treats the kill switch as the primary emergency stop, not a config flag. Confidence: 0.9
- For LLM-as-judge low-confidence verdicts, wants a **redteam auto-spawn** pattern: when primary judge confidence < threshold, spawn N expert sub-agents in parallel from a persona pool, then aggregate via 3-tier agreement policy (unanimous → override primary, majority → escalate human, no consensus → block). Cross-model requirement: judge must use a different model than the builder. Confidence: 0.9
- For optional/configurable layers (judge model, scheduler, hooks), prefers **config-driven with `null = use default`** + env-var override (`AHD_<SLOT>=value`) + graceful fallback to in-memory default when YAML is missing or malformed. Never let optional config crashes the caller. Confidence: 0.9
- When generating IDs across related types (e.g., binary rubric vs score rubric, FR vs NFR), wants **numeric range separation** to avoid collision (binary 001-499, score 500-999) — preferred over a single namespace with type prefix, because ID-driven file paths are simpler to read and test. Confidence: 0.85
- For testable/rubric-driven systems, wants **evidence-collection fixtures** in generated tests (e.g., `evidence_log` pytest fixture writing to `tmp_path`) so every test leaves a verifiable artifact, not just a boolean pass/fail. Confidence: 0.85
- For multi-phase implementations, wants **per-phase test runs with running test-count tally** (e.g., "Phase 0: 33/33", "Phase 1: 14/14") — turns test count into a progress signal, not just a final verdict. Reinforces the smallest-coherent-diff principle. Confidence: 0.8
- For Windows-compatible Python CLI scripts, wants explicit `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` (and stderr) at the top of `__main__` to prevent `UnicodeEncodeError: 'charmap' codec` when printing BRD/error messages that contain non-ASCII (Vietnamese diacritics, emoji). Treats cross-platform encoding as a CLI script invariant, not a runtime concern. Confidence: 0.9
- For cron-style / scheduled-task scripts (e.g., nightly bench, daily cleanup), wants a **date-stamp state file** pattern: `Path(".devin/state/<task>_last_run")` containing today's date; on entry, if file content == today's date → exit 0 (skip). Write/update state file BEFORE doing the work to prevent race-condition double-runs. Add `--state-file` CLI flag so the path is configurable. Confidence: 0.85
- For CLI tools that may need to run in test/CI contexts, wants a `--force` flag that bypasses rate-limit / once-per-day gates (e.g., `auto_pr_runner`'s 1 PR/day rule). Treats the gate as a production safety net, not a test obstacle. Always paired with explicit help text indicating the bypass is for "CI/test" use. Confidence: 0.85
- When moving scripts between safe-zone locations (e.g., `verify_first_cli.py` from root → `scripts/`), wants **systematic call-site updates**: (1) `sys.path.insert` from `Path(__file__).resolve().parent` → `.parent.parent`, (2) test file `ROOT / "x.py"` → `ROOT / "scripts" / "x.py"`, (3) any doc references in SKILL.md / USAGE_GUIDE.md. Treats file moves as atomic: a move is not done until all imports + tests + docs are updated together. Confidence: 0.85
- For BRD-as-source-of-truth schemas, when NFR metric fails the allowlist validation, prefers **fixing the BRD content** (e.g., rename `encoding_handling` → `encoding_score` to match measurable-units allowlist) over expanding the schema's allowlist. Treats the schema as a contract that BRDs must conform to, not a list to grow. Confidence: 0.85
- Wants skill cross-references in skill SKILL.md files: when a new example artifact (e.g., ADVERSARIAL_REVIEW.md, EXECUTION_REPORT.md) is produced, add a one-line reference at the top of related skill docs (e.g., `harness-upgrade/SKILL.md`) so future runs can find the canonical example. Treats the skill doc as a living index, not a one-time write. Confidence: 0.8
- Wants plan artifacts mirrored to workspace for governance compliance: write plan to `C:\Users\<user>\.commandcode\plans\<slug>.md` (user's plan dir) AND mirror to `docs/plans/<slug>/IMPLEMENTATION_PLAN.md` (workspace). The workspace mirror is what `tools/check_governance.py` scans for plan↔act linkage. The user's plan dir is the source of truth; the workspace mirror is the audit anchor. Confidence: 0.9
- Wants CLI entry-point scripts to handle UTF-8 BEFORE any other imports/prints — put the reconfigure call in a try/except (AttributeError-safe for non-standard streams) and run it as the first executable line. Avoids issues where early argparse error messages or help text get the wrong encoding. Confidence: 0.85
- When a subprocess-based wrapper hits repeated failures (e.g., 5 consecutive `command-code chat` failures), wants a **circuit-breaker pattern**: open for cooldown period (default 30 min), reset on success, expose `reset_circuit_breaker()` for test setup. Prevents tight retry loops from cascading into provider-side rate limits. Confidence: 0.85
- For cross-model LLM judge requirements (judge != builder model), wants a **strategy enum** (`cheapest` | `newest` | `rotate`) + helper `pick_cross_model(current, strategy)` that returns a model name from an `available_models` list. Default `cheapest` (Haiku) to minimize cost; `newest` for cutting-edge reviews; `rotate` for variety across runs. Treats cross-model selection as a policy, not a hardcoded value. Confidence: 0.85
- For new CLI tools that may grow features, prefers `argparse` over manual `sys.argv` parsing from day one — even for a single positional arg + 2-3 flags. Cost of `argparse` is ~10 lines, benefit is automatic `--help`, type validation, and free extensibility. Confidence: 0.85
- Wants scripts to be **cross-platform by default** (Windows + macOS + Linux) using a single file, NOT separate files per OS. Cross-platform means: use `sys.platform` (not `os.name`), pick Python binary dynamically (`py` on Windows, `python3` on POSIX), normalize paths, handle shell metachars per-platform. Forbids the pattern of `script_windows.py` + `script_macos.py` as a maintenance burden. Confidence: 0.95
- For cross-platform code that needs to detect Python, prefers a helper module (e.g., `_platform_utils.py`) exposing `get_python_cmd()`, `is_windows()`, `is_macos()`, `is_linux()`, `is_posix()`, `run_python()`, `normalize_path()`, `safe_chmod()`, `ensure_utf8_output()`. Single source for cross-platform logic, no per-script detection. Confidence: 0.9
- For subprocess calls, prefers a wrapper `run_python(args, **kwargs)` that prepends the correct binary and handles env, cwd, timeout — over calling `subprocess.run(["py", ...])` or `subprocess.run(["python3", ...])` inline at call sites. Centralizes the platform detection. Confidence: 0.9
- When given a system redesign directive (e.g., "HLK must be the single source of truth"), wants **3-step process in order**: (1) Compare/diff current state vs target, (2) Clone/merge missing pieces into the canonical source, (3) Replace mirrors with thin pointers/wrappers. Never skips step 1 — even a quick `diff` script run before editing prevents data loss. Confidence: 0.9
- When consolidating to a canonical source, wants the source-of-truth to be **wider/more complete** than the legacy, with the legacy treated as a "ngoại lệ" (exception) — never deletes the legacy, but makes it non-canonical. The legacy keeps its code; the new canonical gets a superset of features. Prevents accidental data loss in migration. Confidence: 0.9
- Wants cross-provider architecture to follow a **layered pattern with strict 1-way sync**: Canonical (HLK) → Mirrors (.devin as exception, .commandcode, .opencode) via a sync script. Sync is idempotent, audited (writes `reports/sync_<date>.jsonl`), and never deletes files in the mirror (only adds/updates). Mirror files are thin shims (1-2 lines re-exporting from canonical) or pointer docs (1 paragraph referencing canonical). Confidence: 0.9
- For skill/command/agent cross-provider registration, wants a **3-tier system**: (1) canonical `HLK/skills/X/SKILL.md` (full implementation), (2) thin pointer files in `.commandcode/`, `.opencode/` (1-2 sentences + canonical reference), (3) sync mechanism (`sync_to_mirrors.py`) to keep them aligned. Providers discover via their native paths but all point to one source. Confidence: 0.9
- For Node.js scripts that need to launch Python CLIs cross-platform, prefers the pattern: `const isWindows = process.platform === "win32"; const pythonCmd = isWindows ? "py" : "python3";` followed by `spawn(pythonCmd, [scriptPath, ...args], { stdio: "inherit" })`. Wraps the audit log + error handling around the spawn. Lives in safe-zone if HLK is blocked. Confidence: 0.85
- When asked to "deploy everywhere" / "wrap all providers" / "register everywhere", wants a single deployment plan that produces: (1) the wrapper/wiring in each provider's native path, (2) a cross-provider matrix table showing what lives where, (3) a sync mechanism to keep them in step, (4) an adversarial review of the wiring (6 personas). Treats cross-provider deployment as one architectural decision, not N independent changes. Confidence: 0.9
- For Node.js hook/loader code that needs to be require'd (PreToolUse patch via `--require`), prefers CommonJS (`.cjs` extension) when `package.json` has `"type": "module"` — the `.cjs` extension forces CommonJS even in ESM projects. Avoids `ERR_REQUIRE_ESM` runtime errors. Confidence: 0.85
- For shim/re-export modules, prefers **explicit imports via `__all__` extraction** over `from X import *` (wildcard) — wildcard imports are a security risk if the canonical module is compromised (attacker can inject any name). Uses AST parsing (not `__import__` exec) to safely extract `__all__` from the source file. Documents the security rationale in the shim's docstring. Confidence: 0.9
- For audit logs (sync runs, gate verdicts, PR creates), wants **per-action JSONL files** with timestamps: e.g., `HLK/reports/sync_<YYYY-MM-DD>.jsonl` with `{timestamp, target, action, src, dst}`. Append-only, easy to grep, doesn't need rotation. Date in filename so daily files accumulate naturally. Confidence: 0.85
- When `AGENTS.md` (root) needs to enforce a workspace-wide rule (like "HLK is the source of truth"), wants the rule expressed as a **numbered hard invariant list** at a clearly-marked section, not buried in prose. Format: "## HLK is Source of Truth (BẮT BUỘC — mọi agent, mọi provider)" followed by 5 numbered rules + code examples + tier-based exceptions. Treats it as a contract for the codebase. Confidence: 0.9
- For `diff_compare.py` / migration tooling, wants output to be **structured markdown** (not just stdout text) — sections like "Section 1: HLK có, .devin KHÔNG có", "Section 2: .devin có, HLK thiếu — CẦN CLONE", with file lists per section. Output file goes to `HLK/docs/migration-diff.md` for git history + governance scan. Idempotent — re-running gives the same result if state hasn't changed. Confidence: 0.85
- For Python AST-based safe introspection (e.g., extracting `__all__` without importing), prefers `ast.parse(content)` + `ast.walk(tree)` over `__import__` or `importlib`. Reasons: (1) doesn't execute module code, (2) works on broken syntax (catches `SyntaxError` gracefully), (3) faster than subprocess. Always wrap in `try/except (SyntaxError, OSError)` to handle missing files. Confidence: 0.85
- For config-loader modules (e.g., `HLK/chain/config.py`), wants **defensive merge with defaults**: if the canonical config file is missing the section, fall back to a hardcoded `defaults` dict; if the canonical is blocked from editing, the defaults still work. Pattern: `merged = {**defaults, **vf}` so user-config overrides defaults but missing keys get sensible values. Confidence: 0.9
- For build/migration scripts that need to run scripts in a fresh state (e.g., re-sync after schema change), wants the **delete-then-create pattern**: `Remove-Item *.py; sync`. This is acceptable even though it looks destructive, because (1) the script is a re-derivation, not the source of truth, (2) the source of truth (HLK) is unchanged. Idempotent sync then re-creates everything. Confidence: 0.8
- When AST-based rewriter scripts (e.g., adding `__all__` to all HLK modules) break syntax due to bad insertion position, prefers **brute-force fix + verify with import test** — remove ALL existing `__all__` blocks, parse to get public names, re-insert at correct position (after all imports), then run `import chain; print(chain.X.__name__)` to confirm no SyntaxError. Iterate until import test passes. Confidence: 0.85
- For pytest conftest issues (e.g., conftest imports a module that was moved during migration), prefers running tests with `--noconftest` flag to skip the broken conftest and isolate the test itself. If tests pass without conftest, the issue is in the conftest's imports, not the tests. Treats as a triage tool, not a permanent solution. Confidence: 0.8
- For deployment/completion reports, wants a **"GoalSpec" section at the top** with explicit checkboxes for each acceptance criterion, marked as `[x]` when satisfied. Format: "**Acceptance criteria**:\n- [x] Phase 0: ...\n- [x] Phase 1: ...\n- [ ] Phase 2: ...". Makes the completion claim auditable at a glance without reading the body. Confidence: 0.9
- For test runs in multi-phase plans, wants **per-phase test counts reported inline** in the report's phase-by-phase summary (e.g., "Phase 1: 21/21 pass", "Phase 5: 10/10 pass") and a final aggregate count. Treats the running tally as progress evidence, not just a final 224/224 verdict. Confidence: 0.85
- For P0 security findings raised in adversarial review, wants them **fixed in the same session before reporting completion** — not deferred to backlog. Example: "wildcard imports are a security hole" → fix by switching to AST-based explicit imports, re-run sync, re-verify. The review→fix→verify cycle is one atomic step in the implementation phase. Confidence: 0.9
- Wants "decision log" tables in deployment docs (Date | Decision | Reason) for non-obvious architectural choices. Example: "2026-08-27 | .devin = ngoại lệ, KHÔNG xóa | User directive: 'không xóa các cài đặt trong .devin'". Lets future maintainers understand WHY a decision was made, not just WHAT was decided. Confidence: 0.85
- When user says "tiếp tục" / "continue" after a completion summary that lists open follow-ups, treats the deferred list as the implicit next-task scope — no re-confirmation of scope, no re-scoping question, just execute the outstanding items in order. Saves a round-trip; the user already implicitly chose by not objecting. Confidence: 0.9
- For sync/copy mechanisms that need to update existing files in place, wants the **state-file pattern with SHA256 hash**: store `{file_key: sha256}` in `.devin/state/sync_state.json`, recompute hash on each run, only rewrite if hash changed. Add `--force` flag to bypass state for re-derivation. Add `--state-file` flag to make path configurable. Treats hash-based skip as O(Δ) instead of O(N) writes. Confidence: 0.9
- For Python packages with many submodules, prefers **PEP 562 lazy import** in `__init__.py` via `__getattr__(name)` + `_LAZY_MODULES: dict[str, str]` mapping (name → module). Uses `importlib.import_module()` + `globals()[name] = value` for caching after first access. Keeps `__all__ = sorted(_LAZY_MODULES.keys())` so `from chain import *` still works. Also add `__dir__()` so `dir(chain)` lists all public symbols without forcing load. Reduces import cost from O(N modules) to O(1 module) for single-symbol use. Confidence: 0.9
- For Node.js audit log rotation, prefers **rename-with-date+timestamp suffix** pattern: when log file > threshold (default 10MB), `renameSync(logPath, archivePath)` where `archivePath = parent/${base}_${YYYY-MM-DD}_${Date.now()}${ext}`. Make threshold configurable via env (`AHD_AUDIT_MAX_SIZE_MB`). Wrap in try/catch so a failed rename doesn't break the caller. Then create a fresh empty log file on next write. Confidence: 0.85
- For CI workflows adding cross-platform testing, wants the **matrix appended as a new job** (not modifying existing jobs) with `fail-fast: false` + `[ubuntu-latest, macos-latest, windows-latest]`. Use `actions/setup-python@v7` + `actions/checkout@v7` for current major versions. Add a comment in the YAML marking which plan/phase added it. Validates the YAML by `yaml.safe_load` after writing. Confidence: 0.85
- For new plan-driven work after a "tiếp tục" continuation, wants a **fresh plan artifact** written to a new slug (e.g., `hardening-p1.md`) with its own GoalSpec / Phases / Files / Verification sections, NOT an in-flight edit of the previous plan. Each plan = one session's atomic commitment. Mirror the plan to `docs/plans/<slug>/IMPLEMENTATION_PLAN.md` immediately after writing to user's plan dir. Confidence: 0.85
