Bạn đang chạy skill `hlk-loop` (HLK pipeline tự học) qua cmdc.

1. Đọc `.devin/skills/hlk-loop/SKILL.md` (source of truth, HLK pipeline protocol).
2. Subcommands: `status | dry-run | reset | iterate`.
3. Wrapper: `node HLK/wrappers/hlk-loop.mjs <subcommand>`.
4. Không ghi đè state HLK. Không force-push. Không destructive.

Action: $ARGUMENTS
