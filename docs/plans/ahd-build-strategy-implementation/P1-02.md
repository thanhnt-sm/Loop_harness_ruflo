# SUB-PLAN — P1-02: Structured-error MCP layer + circuit breaker

> **Source**: `docs/research/BUILD_PRODUCTION_PAIN_POINTS_2026.md` (P1 section) + Backlog REQ-P1-02
> **Priority**: 🔴 HIGH (URGENT - Security + Governance Decay T4 A1)
> **Date**: 2026-08-27

---

## 1. Objective

Implement **Structured-error MCP layer** (SERF - Structured Error Response Format) + **client-side circuit breaker** để eliminate silent partial failures:

- MCP tools **MUST** return structured `{ok, error, detail}` + `completeness` field
- Client-side circuit breaker trips khi upstream fail > threshold
- No silent `null` responses interpreted as success

---

## 2. File Paths & Functions

| File Path | Type | Function |
|-----------|------|----------|
| `.devin/mcp_config.json` | Audit | Verify existing MCP servers, ensure contract compliance |
| `.devin/hooks/post_tool_mcp_guard.py` | **New** | Hook wrapper for MCP tool calls → enforce structured response + circuit breaker |
| `.devin/hooks/post_tool_config.py` | Extend | Add MCP guard config constants |
| `tests/test_mcp_guard.py` | **New** | Unit tests for MCP guard |

---

## 3. Acceptance Criteria (REQ-P1-02)

| AC | Description | Verification |
|----|-------------|--------------|
| **AC-1** | MCP tool calls wrapped to enforce `{ok: bool, error: str|null, detail: any, completeness: float}` | Unit test: call wrapped tool → assert response shape |
| **AC-2** | Circuit breaker trips after N consecutive failures (configurable, default 3) | Unit test: simulate 3 failures → assert breaker open → fallback triggered |
| **AC-3** | No silent `null` responses — any `null` converted to `{ok: false, error: "null_response", ...}` | Unit test: tool returns null → assert structured error |
| **AC-4** | Completeness field ∈ [0.0, 1.0] indicates response quality | Unit test: assert completeness present and bounded |
| **AC-5** | Circuit breaker auto-resets after cooldown period (configurable, default 60s) | Unit test: wait cooldown → assert breaker half-open → success closes |
| **AC-6** | Integration with existing post_tool_use hook chain | Integration test: real MCP call → verify guard fires |

---

## 4. Implementation Details

### 4.1 post_tool_mcp_guard.py (New Hook Module)

```python
"""MCP Guard — Structured Error Response Format (SERF) + Circuit Breaker.

Wraps MCP tool calls to enforce:
  1. Structured response: {ok, error, detail, completeness}
  2. Client-side circuit breaker (trip on N failures)
  3. No silent null responses
"""

from __future__ import annotations
import time
import threading
from dataclasses import dataclass, field
from typing import Any, Callable, Optional
from pathlib import Path

# Config (import from post_tool_config or define here)
MCP_CIRCUIT_BREAKER_THRESHOLD = 3
MCP_CIRCUIT_BREAKER_COOLDOWN = 60  # seconds
MCP_DEFAULT_COMPLETENESS = 1.0


@dataclass
class CircuitBreakerState:
    failures: int = 0
    last_failure_time: float = 0.0
    state: str = "closed"  # closed, open, half-open
    _lock: threading.Lock = field(default_factory=threading.Lock)

    def record_success(self):
        with self._lock:
            self.failures = 0
            self.state = "closed"

    def record_failure(self):
        with self._lock:
            self.failures += 1
            self.last_failure_time = time.time()
            if self.failures >= MCP_CIRCUIT_BREAKER_THRESHOLD:
                self.state = "open"

    def can_execute(self) -> bool:
        with self._lock:
            if self.state == "closed":
                return True
            if self.state == "open":
                if time.time() - self.last_failure_time >= MCP_CIRCUIT_BREAKER_COOLDOWN:
                    self.state = "half-open"
                    return True
                return False
            # half-open
            return True


# Global circuit breakers per MCP server
_MCP_CIRCUIT_BREAKERS: dict[str, CircuitBreakerState] = {}
_CB_LOCK = threading.Lock()


def _get_breaker(server_name: str) -> CircuitBreakerState:
    with _CB_LOCK:
        if server_name not in _MCP_CIRCUIT_BREAKERS:
            _MCP_CIRCUIT_BREAKERS[server_name] = CircuitBreakerState()
        return _MCP_CIRCUIT_BREAKERS[server_name]


def enforce_structured_response(
    server_name: str,
    tool_name: str,
    raw_response: Any,
) -> dict:
    """Convert any MCP response to SERF format."""
    # If already structured, validate and return
    if isinstance(raw_response, dict) and "ok" in raw_response:
        result = {
            "ok": bool(raw_response.get("ok", False)),
            "error": raw_response.get("error"),
            "detail": raw_response.get("detail"),
            "completeness": float(raw_response.get("completeness", MCP_DEFAULT_COMPLETENESS)),
        }
        # Clamp completeness
        result["completeness"] = max(0.0, min(1.0, result["completeness"]))
        return result

    # Null response → structured error
    if raw_response is None:
        return {
            "ok": False,
            "error": "null_response",
            "detail": f"MCP server '{server_name}' tool '{tool_name}' returned null",
            "completeness": 0.0,
        }

    # Primitive/other → wrap as success with detail
    return {
        "ok": True,
        "error": None,
        "detail": raw_response,
        "completeness": MCP_DEFAULT_COMPLETENESS,
    }


def mcp_guard_call(
    server_name: str,
    tool_name: str,
    call_fn: Callable[[], Any],
) -> dict:
    """Execute MCP tool call with circuit breaker and SERF enforcement."""
    breaker = _get_breaker(server_name)

    # Check circuit breaker
    if not breaker.can_execute():
        return {
            "ok": False,
            "error": "circuit_breaker_open",
            "detail": f"Circuit breaker OPEN for MCP server '{server_name}' (failures: {breaker.failures})",
            "completeness": 0.0,
        }

    try:
        raw = call_fn()
        structured = enforce_structured_response(server_name, tool_name, raw)

        if structured["ok"]:
            breaker.record_success()
        else:
            breaker.record_failure()

        return structured

    except Exception as e:
        breaker.record_failure()
        return {
            "ok": False,
            "error": "exception",
            "detail": f"MCP call failed: {type(e).__name__}: {e}",
            "completeness": 0.0,
        }


def get_circuit_breaker_status(server_name: str) -> dict:
    """Get current circuit breaker status for monitoring."""
    breaker = _get_breaker(server_name)
    with breaker._lock:
        return {
            "server": server_name,
            "state": breaker.state,
            "failures": breaker.failures,
            "last_failure": breaker.last_failure_time,
        }
```

### 4.2 Integration with post_tool_use

In `post_tool_engine.py` or `post_tool_enforce_quality.py`, add MCP guard invocation for MCP tool calls (tool_name starts with `mcp__`).

---

## 5. Test Plan

| Test | File | Method |
|------|------|--------|
| Structured response enforcement | `tests/test_mcp_guard.py` | `test_enforce_structured_ok`, `test_enforce_structured_null`, `test_enforce_structured_primitive` |
| Circuit breaker trip | `tests/test_mcp_guard.py` | `test_circuit_breaker_trips_after_threshold` |
| Circuit breaker cooldown/reset | `tests/test_mcp_guard.py` | `test_circuit_breaker_cooldown_reset` |
| Completeness field bounds | `tests/test_mcp_guard.py` | `test_completeness_bounds` |
| Integration with hook | `tests/test_mcp_guard.py` | `test_integration_mcp_call` |

---

## 6. Governance Compliance

- ✅ Files in allowed paths: `.devin/hooks/`, `tests/`
- ✅ New hook follows existing pattern (post_tool_*)
- ✅ Test file in `tests/`
- ✅ No root markdown created
- ✅ Will run `python tools/check_governance.py` after implementation

---

## 7. Risks

| Risk | Mitigation |
|------|------------|
| Breaking existing aide-memory MCP calls | Test with actual aide-memory server; fallback to permissive mode initially |
| Circuit breaker too aggressive | Configurable threshold/cooldown; start conservative (3/60s) |
| Performance overhead | Minimal — only wraps MCP calls, fast path for non-MCP tools |

---

*Sub-plan ready for implementation | 2026-08-27*